import React from 'react';
import { FancyCheckbox } from '../../../..';
import { checkableTests } from '../../../../test/utils';

describe('FancyCheckboxComponent', () => {
    checkableTests(FancyCheckbox);
});
