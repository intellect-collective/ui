import React from 'react';
import { Checkbox } from '../../../..';
import { checkableTests } from '../../../../test/utils';

describe('Form Components - CheckboxComponent', () => {
    checkableTests(Checkbox);
});
